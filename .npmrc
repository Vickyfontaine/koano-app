## @clerk/nextjs v7 declares a peer dep on Next 15+; KOANO is on Next 14.2
## (App Router, works fine). Without this flag npm install fails with
## ERESOLVE on Vercel and on fresh local clones.
legacy-peer-deps=true
